// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        maven { url 'https://nexus.taojiji.work/nexus/content/groups/public/' }
        maven { url 'http://192.168.30.151:8081/nexus/content/repositories/ocss-release/' }
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.1'
        classpath 'com.github.jokar:multi-language.plugin:0.2.9-SNAPSHOT'
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        maven { url 'https://nexus.taojiji.work/nexus/content/groups/public/' }
        maven { url 'http://192.168.30.151:8081/nexus/content/repositories/ocss-release/' }
        jcenter()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
